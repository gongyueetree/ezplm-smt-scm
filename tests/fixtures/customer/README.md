# 客户真实样例(不入版本库)

客户提供的真实/脱敏失败样例(BOM、Gerber、对账单)放在本目录,
`.gitignore` 已排除 —— **客户文件绝不进 git**。

用途:拿到 O10/O11 的真实样例后,在本地把它们丢进来,
跑 `pnpm test:golden` 前手工对照 golden 夹具复现;
确认复现后**用合成数据重写等价夹具**入库(如 zz-e9 之于 15MB BOM),
原件留在这里,不提交。
