本目录刻意为空:大文件夹具**运行时生成、不入 git**。

- 快速档:10,500 行混合脏数据(固定种子,pnpm test:golden 每次都跑);
- 慢速档:>10MB(GOLDEN_SLOW=1 才跑;同场景的 HTTP 全链路由 tests/e2e/zz-e9 在每次 CI 覆盖)。

期望值由 tests/golden/seeded.ts 的生成器**边生成边记账**推导 ——
与解析结果是两个独立来源,对得上才算数;跑一遍抄下来的快照测不出任何东西。
