# 报告 9 · 链接和脚本检查结果

由 `check-v4.py` 检查（两版 + phase2-preview）。结果 EXIT=0。

## 链接
- 共扫描 **135 个** `href="*.html"` 链接。
- 断链 = **0**。

## 脚本引用
- 所有 `<script src="*.js">` 引用的文件 = **全部存在**（缺脚本 0）。
- 客户确认版实际加载 JS：`contract.js`、`nav-contract.js`。
- 客户确认版未加载的旧 JS（rbac.js / workflow.js / ui-helpers.js）= **已删除，无残留**。

## JS 语法
- 检查 **65 段** JS（外部 JS + 各页内联 `<script>`）。
- 语法错误 = **0**（使用 Node.js `--check`；若运行环境无 Node 则此项跳过并提示）。

## 移动端导航
- `nav-contract.js`（customer / internal / phase2-preview 三处）均含 `ez-hamburger`、`ez-nav-overlay`、`Escape` 关闭、`injectMobileNav`。
- 两版 `contract.css` 不再于窄屏直接隐藏 `.sidebar`（改由抽屉接管）。
- 建议在 1440 / 1024 / 768 / 390px 断点人工复验抽屉开合与遮罩/Esc 关闭。

---
## V5 补充
- check-v5 §17 以 Node 运行时桩实测 ezExportCSV，确认 25 个调用点（15 两参 + 10 三参）均不抛错。
- §18 **静态**校验 Tab 处理器兼容 `.pane`/`.tab-pane`，且每个 tab 的 data-pane 在同组面板中有唯一目标，CSS 含 `.pane.active`。
- 浏览器交互测试（Tab 点击、状态机流转、人工修改写回）由**开发侧用 jsdom 单独执行**，不属于 check-v5.py；正式交付前建议用 Playwright/Cypress 在真实浏览器复验。
