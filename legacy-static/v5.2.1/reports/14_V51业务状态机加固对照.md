# 报告 14 · V5.1 业务状态机加固对照（基于 GPT 第二轮诊断）

本轮不新增页面，专注业务闭环与状态一致性。check-v5_1.py 新增 §27–§32 校验，并在 V3 上 EXIT=1 命中全部缺口、在 V5.1 上 EXIT=0。

## P1-1/2/3 物料报价重新询价闭环（material-quote.html）
- 异常处理结论**默认空**（"请选择处理结论"），删除程序预选，须逐项人工选择。
- REQUOTE / 更换货源 / 调整价格：须采购填写**新价**并通过**重新校验**（新价仍超线则 `purchaserDone=false`，退回继续处理）。
- 角色分离：采购处理价格异常（handledBy）→ PM 确认结果（confirmedBy）→ 管理人员审批；步骤条由 6 步扩为 7 步。
- 进入 PM 确认前校验 `flaggedLines().filter(l=>!l.purchaserDone)` 为空。
- GTB 提示改"不低于 MOQ 并按 SPQ 向上圆整"。
- 验证：开发侧 jsdom — 未选结论被拦、REQUOTE 未填新价被拦、正确处理后放行。

## P1-4/P1-5 报价审批状态机与分类门禁（quote.html）
- 分类行改为 `quoteLines` 状态数组（PENDING/CONFIRMED/CORRECTED）；fixCls 真正写回状态、修正原因必填并留痕。
- 报价状态机 DRAFT→PENDING_APPROVAL→APPROVED/REJECTED；页面初始 DRAFT。
- submitApprove 门禁：分类未全部确认（`clsAllDone()`）不得提交；非 DRAFT 不可重复提交。
- PDF / 正式报价单导出仅 APPROVED 后可用；草稿导出标注"草稿·非正式"。
- 验证：jsdom — 未确认提交被拦（2→0 后放行）、未批准 PDF 被拦、批准后放行。

## P1-6/P1-7 计划排产（planning.html）
- 引入 `PLAN_DATE` 基准日；`materialReady = readyAt <= PLAN_DATE`。
- 所有工单（含未来齐料）均进入排产并有顺位、建议开始（不早于齐料日）、预计完成。
- 工序字段 route/completedOps 参与展示与就绪判定（前序未完成则不并入可上线）。
- 验证：jsdom — 4 行全有顺位；WO-7741 在其 07-18 齐料日才开始。

## P1-8 OPO 统一状态（opo.html）
- 单一 `OPO` 状态对象（suppliers/replied/abnormal…），`renderOpo()` 统一渲染 KPI + 未回复供应商表 + 脚注。
- simulateReply / portalSubmit 改状态后统一 render；portalSubmit 防重复。
- 验证：jsdom — 未回复 KPI 恒等于供应商表合计（23→14→13），脚注/异常同步，重复提交被防护。

## P1-9 角色（ar-ap.html）
- 移除范围外"品质"角色，改"采购 / 管理人员"。

## 工程/报告问题
- supplier.html：OPO 门户 Tab 增加"打开完整供应商门户页"入口，消除孤立页（§31）。
- 报告措辞更正：check-v5/v5.1 为**静态结构 + 运行时桩**校验；Tab 点击、状态机流转等**浏览器交互测试由开发侧 jsdom 单独执行**，非 check 脚本内置；正式交付前建议 Playwright/Cypress 真实浏览器复验。
- 交付页"下载全部模板"标注多文件下载可能被浏览器拦截，正式系统提供 ZIP / 多 Sheet XLSX。
- 报价规则区标注"原型示例，最终以甲方确认的报价规则表为准"。

## check-v5_1.py 新增校验（§27–§32）
27 报价状态机与提交门禁 · 28 重新询价闭环 · 29 计划排产基准日/未来齐料 · 30 OPO 统一状态渲染 · 31 无孤立页 · 32 无范围外角色。
- V5.1：32 项全过，EXIT=0。
- V3：EXIT=1，新检查命中状态机缺失、purchaserDone 门禁缺失、OPO 未统一、孤立 supplier.html、品质角色等。
