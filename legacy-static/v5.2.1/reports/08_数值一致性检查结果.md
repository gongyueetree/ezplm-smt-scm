# 报告 8 · 数值一致性检查结果

由 `check-v4.py` 按页面规则**重新计算**并与显示值核对（非关键词检查）。结果 EXIT=0。

## 报价页（quote.html，两版一致）
- 计算标准单价 = **¥347.46**；B 模式 95 折后单价 = **¥343.15**。
- KPI、计算明细、折扣模拟、报价历史当前行均绑定同一 `recalc()` 结果。
- 订单金额 = 单价 × 3000，随折扣/模式同步更新。
- 一致性自检：所有"当前单价"相同；总额 = 单价 × 数量；折扣前后关系正确。
- KPI 写死 ¥382.6 = **已移除**；审批链角色模型外审批人 = **0**。

## 物料报价（material-quote.html，两版一致）
- 按 `low`、`FILL`、`THRESH=5%` 重算，实际异常 = **3 项**：
  - STM32F103C8T6（8.45 > 历史低价 8.20）→ 高于历史低价
  - GRM188R71H104KA93D（0.0205 > 0.018×1.05）→ 超阈值
  - PESD5V0S1BA（0.118 > 0.11×1.05）→ 超阈值
- 状态徽标/提示/弹窗数量同源（`flaggedLines()`）；此前遗漏的 GRM188R71H104KA93D 已纳入弹窗与异常计数。
- 写死"2 项触发提醒" = **已移除**。

## 计划辅助（planning.html，两版一致）
- 公式：`SMT点×数量×秒/3600 + DIP位×数量×秒/3600 + 数量×检测秒/3600 + 换线次×分/60`。
- WO-7735 公式重算 = **164.9h**（系统计算），人工确认 = 160.0h，分列展示。
- 规则（SMT 0.45 / DIP 4.2 / 检测 35 / 换线 30）可编辑，修改后所有工单自动重算。
- 写死 61.5h = **已移除**。

## OPO（opo.html，两版一致）
- 错误 cron `0 9 * * 1/2` = **已移除**；改"自起始日起每 14 天"。
- 周期字段齐备：起始 2026-05-18 / 上次 06-01 / 下次 06-15 / 截止 06-15（无矛盾）。
- 旧截止日 06-14 = **已统一为 06-15**。

## 系统集成（integration.html，两版一致）
- 违规状态词（已启用/已试运行/已回写/已同步/已自动重试/延迟 86ms/试运行）= **无**。
- "待客户 IT 确认"声明 = **有**；金蝶具体版本 K/3 WISE 断言 = **已移除**。

---
## V5 补充（运行时实测）
- ezExportCSV 三种调用形态 + 垃圾输入：运行时异常 **0 次**（check-v5 §17 自动实测）。
- 物料报价 GTB 含 MOQ+SPQ；逐项 decision 闭环要求全部异常给出结论方可提交（§20/§23）。
- 计划 fixHours 真正写回 WOS 并重算；排产纳入工序就绪判定（§21/§22）。
  - 注：check-v5.py 做静态结构校验；KPI 523h→526h 等交互结果由开发侧 jsdom 单独验证，不属于 check-v5.py。
- OPO simulateReply 后 KPI（异常 8→10）/差异表/异常清单一致（§24）。
