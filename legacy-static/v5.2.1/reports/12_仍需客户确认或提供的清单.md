# 报告 12 · 仍需客户确认或提供的数据/接口/模板清单

## 一、ERP/MES 与接口（待客户 IT 确认）
1. 现有金蝶 ERP/MES 的**具体版本**与模块。
2. 是否开放 **API**；若不开放，确认可用替代：中间表写权限、数据库**只读视图**授权、或仅 **Excel/RPA**。
3. 各业务对象的**字段映射**确认（库存/采购 PO/销售 PO/工单/OPO 等），以冻结接口路径与字段。
4. 同步调度策略（频率）、失败处理与待办归属。
5. 真实**邮件系统**账号、SMTP/API 授权与网络环境（供 OPO 催复、Call 料邮件使用）。

## 二、测试与账号
6. 联调用**测试库/测试账号**与权限。
7. 各角色（PM/采购/计划/管理/供应商）演示账号确认。

## 三、样例数据
8. 样例 **BOM**（标准 + 非模板 + 含/不含 MPN）、含图片/扫描件样例（供 OCR）。
9. 样例**客户 PO、采购 PO、工单、库存、OPO、ETA**。
10. 样例**对账单**（客户 AR / 供应商 AP）。

## 四、业务规则
11. 报价规则：A/B/C 客户模式参数、算点规则、管理费率、毛利红线。
12. 采购规则：历史最低价口径、超价阈值（默认 +5% 待确认）、MOQ/SPQ 来源。
13. 工时规则：SMT/DIP/检测/换线标准工时（默认 0.45/4.2/35/30 待确认）。
14. 对账规则与各模块审批规则（审批人、升级条件）。

## 五、模板确认（原型已提供 CSV，正式系统生成 XLSX）
15. BOM 模板、报价模板、物料报价模板、OPO 回复模板、AR/AP 对账模板、Call 料表模板的**最终字段与格式**。

## 六、范围与商务
16. phase2.html 中 16 项超范围需求如需纳入，须**另行确认范围、费用和周期**（二期/变更）。

> 以上确认项汇总于客户确认版 `delivery-acceptance.html`（⑤ 客户待提供资料），可随验收推进逐项勾选。
