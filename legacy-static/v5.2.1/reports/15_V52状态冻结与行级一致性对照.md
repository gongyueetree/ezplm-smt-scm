# 报告 15 · V5.2 状态冻结与行级一致性对照（基于 GPT 第三轮诊断）

本轮不新增页面，聚焦两个 P0 与若干业务一致性。check-v5_2.py 新增 §33–§38 **jsdom 运行时行为断言**，逐项以真实 DOM 执行验证（非仅源码关键词）。

## P0-1 已批准/审批中报价必须冻结（quote.html）
- 提交审批保存 `submittedSnapshot`，审批通过保存 `approvedSnapshot`（含 mode/discount/各成本/单价/总额/quoteLines/提交人/时间/版本号）。
- PENDING_APPROVAL 与 APPROVED 状态下：客户类别、折扣、分类「修正」按钮、导入 BOM 全部禁用，页面显示冻结提示。
- 正式 PDF 与导出**使用快照值**，不取实时输入。
- 需修改 → 「创建修订版」回到 DRAFT（版本 +1），原快照保留，不覆盖已批准版本。
- 运行时断言 §33：审批通过后折扣输入 disabled；将折扣拖到 80% 后导出价仍为锁定的 ¥343.15（不漂移）。

## P0-2 单产线排产时间重叠与 16h 工作日（planning.html）
- 游标由日期字符串改为完整 `DateTime`；新增 `advanceWorkingHours`：每天两班 08:00–16:00、16:00–24:00（16h），跨天顺延。
- 下一工单开始 = max(上一工单实际完成, 物料齐料日, 工序就绪)。
- 运行时断言 §35：相邻工单无重叠；最大工单自然时长 ≥ 工时（含夜间停工）；4 工单连续顺位。

## P1 业务一致性
| # | 问题 | 修复 | 运行时断言 |
|---|---|---|---|
| 退回死路 | REJECTED 后无法继续 | 记录退回原因；「创建修订版」回到 DRAFT 可重提 | §34 |
| 新价输入需失败一次 | 首次打开无输入框 | 始终渲染新价/新供应商输入，选 REQUOTE 即时启用 | §38 |
| PM 确认遗漏 | 调整成功的行从 flaggedLines 消失，未被确认 | 以 `wasFlagged` 原始异常集合为准，PM 确认覆盖全部 | §38 |
| 状态文案 | 用「当前超线数」 | 改用 `unresolvedCount`（未处理数） | §28（静态）|
| 更换货源 | 只改价 | 记录 newSupplierName/leadType/quoteDate | §38 |
| 工序未参与 | completedOps 仅展示 | `remainingHours` 按剩余工序扣减，真正影响排产 | §36（29.7h→19.8h）|

## P1-10/P1-11 OPO 行级统一状态模型（opo.html）
- 建立 `opoLines[]`（opoLineId/供应商/MPN/需求日/原ETA/回复ETA/status/影响工单…）。
- KPI、未回复供应商表、交期差异表、异常清单、门户行**全部派生**：`pendingLines/repliedLines/abnormalLines/pendingBySupplier`；差异/异常表不再有静态 HTML 行。
- 异常由 `回复ETA 晚于需求日 ≥7 天` 或 `超期未回复` 实时判定，不再单独存计数。
- 门户提交 OPO-5113-02 后，按回复结果重算：若新 ETA 不再 ≥7 天，则该行**自动从异常清单移除**，KPI 同步下降——修复 GPT 指出的「异常 KPI 减 1 但异常表仍 3 行」。
- 运行时断言 §37：初始/门户提交后/批量回填后，三处快照均满足 `未回复=供应商表合计`、`已回复=差异表行数`、`异常KPI=异常表行数`；门户提交后异常表不再含 OPO-5113-02。

## 自检脚本升级（回应 §12）
- §33–§38 用 jsdom 真正执行交互并断言结果，而非源码关键词：冻结/快照不漂移、退回可修订、排产无重叠/跨天、completedOps 生效、OPO 三表一致、新价即时显示与全异常确认。
- check-v5_2.py：V5.2 全 38 项通过 EXIT=0；原始 V3 EXIT=1，§33–§38 均命中（含 §37 复现 V3 的 KPI 不一致 [23,18,8]→[14,27,8]）。
- 局限与建议：jsdom 为无头 DOM，更接近真实运行但仍非真实浏览器；正式交付前建议用 Playwright/Cypress 复验：报价批准后改折扣被禁止、退回后可重提、REQUOTE 即时显示新价、所有原始异常有 PM 确认、排产不重叠、completedOps 影响排产、OPO 门户提交后异常行消失、KPI/差异/异常表由行级数据重算。
